import React from 'react';
const style = {
    textTransform:'uppercase',
    letterSpacing: '9px',
    fontWeight: '600'

}
const Logo = (props) => (
    <h1 style={style}>todo</h1>
);

export default Logo;
